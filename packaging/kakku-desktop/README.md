# kakku-desktop

Meta-package for the default KakkuOS desktop package set.

Build from this directory after `kakku-niri-settings` is available locally or in a configured package repository:

```bash
makepkg -si
```
